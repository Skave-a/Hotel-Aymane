import { FILTER_MESSAGES } from '../../utils/constants';
import { IFilter } from '../../utils/types'
import styles from "./RoomFilter.module.css";

export default function RoomFilter(props: IFilter) {
  return (
    // <div>{FILTER_MESSAGES}</div>
  )
}
